week02
- linux command : ls, ls -al, pwd, cd .git, cd .., echo "blah" >> README.md, cat README.md
- git command
	git init
	git status
	git add week01.txt
	git add .
	git config --global user.email "your@example.com"
	git config --global user.name "your name"
	git commit -m "v0"
	git log
	git checkout hashcode
	git checkout -
	git branch -M main	
	git remote add origin https://github.com/standhj/test.git
	git remote -v
	git push -u origin main
	git clone (link)
	git clone (link) .
